{
  "name": "control-iperc",
  "version": "1.3.0",
  "description": "Control de Inspecciones IPERC - Cerro Lindo",
  "scripts": {
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "icons": "npx capacitor-assets generate --android --iconBackgroundColor \"#12254c\" --splashBackgroundColor \"#12254c\""
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
